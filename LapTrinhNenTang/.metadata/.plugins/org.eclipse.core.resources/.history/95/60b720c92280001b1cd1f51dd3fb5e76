import java.util.Scanner;

public class XuLy {

	final static char A = 4;
	final static char E = 3;
	final static char G = 6;
	final static char I = 1;
	final static char O = 0;
	final static char S = 5;
	final static char Z = 2;


	public static void main(String[] args) {
		
		Scanner scan = new Scanner(System.in);
		String str = scan.nextLine();
		System.out.println("Chuoi la: " + str);
		char array[] = new char[str.length()];
		for(int i=0; i<str.length();i++) {
			array[i] = str.charAt(i);
			System.out.println(array[i]);
			if(array[i] == 'A') {
				array[i] = A;
			}
			else if(array[i] == 'E') {
				array[i] = E;
			}
			else if(array[i] == 'G') {
				array[i] = G ;
			}
			else if(array[i] == 'I') {
				array[i] = I;
			}
			else if(array[i] == 'O') {
				array[i] = O;
			}
			else if(array[i] == 'S') {
				array[i] = S;
			}
			else if(array[i] == 'Z') {
				array[i] = Z;
			}
			else{
				array[i] = 0;
			}
			System.out.print(array[i]);
		}
		
	}
}
